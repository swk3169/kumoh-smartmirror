<!DOCTYPE html>
<html lang="ko">
  <head>
    <title>Kumoh Smart에 오신것을 환영합니다</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <h1><%= title %></h1>
    <% if(member != "로그인이 필요합니다.") { %>
      <img style="border: 3px solid gold; border-radius: 70px; -moz-border-radius: 70px; -khtml-border-radius: 70px; -webkit-border-radius: 70px; "src="<%=image_address%>">
      <p><%= member %></p>
      <button class="btn btn-success pull-right" data-toggle="modal" data-target="#createAgent"><i class="fa fa-plus-circle" aria-hidden="true"></i>      Create Agent</button>
      <ul>
        <li><a href="/logout">로그아웃</a></li>
        <li><a href="/member_information">내 정보 보기</a></li>
      </ul>
      <% if(isClassification =="false") {%>
      <script type="text/javascript">
        alert("Kumoh Smart의 모든 기능 이용을 위해 회원 세부 정보를 입력해주세요!");
      </script>
      <%}%>

      <% } else if(member == "로그인이 필요합니다."){ %>
        <p><%= member %></p>
        <button class="btn btn-success pull-right" data-toggle="modal" data-target="#createAgent"><i class="fa fa-plus-circle" aria-hidden="true"></i>      Create Agent</button>
        <ul>
          <li><a class="btn btn-block btn-lg btn-warning btn_login" href="/auth/login/kakao">카카오계정으로 로그인</a></li>
        </ul>
    <%} %>
  </body>
</html>
